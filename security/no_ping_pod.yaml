apiVersion: v1
kind: Pod
metadata:
  name: security-context-demo
spec:
  containers:
  - name: no-ping-pod
    image: gcr.io/google-samples/node-hello:1.0
    securityContext:
      capabilities:
        add: ["NET_ADMIN", "SYS_TIME"]